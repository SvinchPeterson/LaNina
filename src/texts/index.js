import { Block, Img, Flex, Link, Text } from '@rackai/symbols'

export const laninaResidence = {
  proto: Block,
  h4: {
    proto: Text,
    props: { text: 'La Nina' }
  },
  span: {
    proto: Text,
    props: { text: 'Residence' }
  }
}
