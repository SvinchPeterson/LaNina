'use strict'

import { Img, Button, Flex } from 'smbls'

const props = {
  boxSize: '100% 100%',
  position: 'absolute',
  top: '0',
  left: '0',
  transition: 'opacity .5s ease-in-out'

}

export const TourContainer = {
  props
}
