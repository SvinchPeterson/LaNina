'use strict'

export const PageTitle = {
  tag: 'h1',
  props: {
    fontSize: 'F',
    textTransform: 'capitalize',
    letterSpacing: `${-3 / 40}em`,
    color: 'cream',
    whiteSpace: 'nowrap'
  }
}