'use strict'
import { Block, Button, Img, Text, Flex, Shape } from '@rackai/symbols'

import Logo from '../../assets/logo.png'
// backgroundImage: 'url(' + CITY_JPEG + ')',
// backgroundSize: 'cover',
// filter: 'grayscale(100%))'
const logo = {
  proto: Img,
  props: { src: Logo }
}
